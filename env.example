# Database Configuration
# For MongoDB (current setup)
DATABASE_URL="mongodb+srv://dayesh1996:4WginRcDb154BMfF@school-management.uecf6hh.mongodb.net/school_management?retryWrites=true&w=majority"

# For PostgreSQL (when using Docker)
# DATABASE_URL="postgresql://dygus_user:dygus_password@postgres:5432/dygus_db?schema=public"

# Application Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration (if using authentication)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Redis Configuration (for Docker setup)
REDIS_URL=redis://redis:6379

# PostgreSQL Configuration (for Docker setup)
POSTGRES_DB=dygus_db
POSTGRES_USER=dygus_user
POSTGRES_PASSWORD=dygus_password

# Application URLs
APP_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Logging
LOG_LEVEL=debug

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# File Upload (if needed)
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads
